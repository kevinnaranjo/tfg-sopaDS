{% extends 'base/base.html.twig' %}

{% block body %}
    <div id="logo" style="width: 30%">
        <a id="logoInicio" href="inicio"> <img src="assets/version_max_red_2t.svg"></a>
    </div>
    <div>
        <h1  class="title-xl"> FSO - Fundamentos de los Sistemas Operativos
        </h1>
    </div>
    <nav aria-label="Breadcrumb" class="ulpgcds-breadcrumb ml-5 pb-5">
        <div>Te encuentras en:</div>
        <ul>
            <li class="ulpgcds-breadcrumb__item"><a class="ulpgcds-breadcrumb__item__link first" href="inicio">ULPGC · Fundamentos de los Sistemas Operativos </a></li>
            <li class="ulpgcds-breadcrumb__item">ULPGC · Exámenes resueltos desde el año 1993</li>
        </ul>
    </nav>

    <H2 class="ml-5 title-xl color-orange">Exámenes resueltos desde el año 1993</H2>

    <form class="ulpgcds-form ml-5">
        <div class="ulpgcds-form__item ulpgcds-form__item--type-select">
            <label for="dropdown">Label</label>
            <select id="dropdown" name="dropdown" onchange="redirigir()">
                <option value="2021">2021</option>
                <option value="2020">2020</option>
                <option value="2019">2019</option>
            </select>
            <div class="help-text">Help text goes here</div>
        </div>
    </form>
    <ul class="ml-5 mr-5 ulpgcds-tabs">
        <li class="active" href="#tab-1">Examen Parcial</li>
        <li href="#tab-2">Convocatoria ordinaria</li>
        <li href="#tab-3">Convocatoria extraordinaria</li>
        <li href="#tab-4">Convocatoria especial</li>
    </ul>

        {% if examenes is empty %}
            No hay examenes disponibles
        {% else %}
            {% set partes = 0 %}
            {% set partesExtraordinaria = 0 %}
            {% set partesEspecial = 0 %}
            {% for examen in examenes %}
                {% if examen.convocatoria == 0 %}
                <div class="ml-5 mr-5 ulpgcds-tab-content active" id="tab-1">
                    {% if examen.fecha  is null %}
                        <h3>Examen Parcial ({{ examen.anno }}):</h3>
                    {% else %}
                        <h3>Examen Parcial ({{ examen.fecha|date("d/m/Y")}}):</h3>
                    {% endif %}
                    <ul class="ulpgcds-list">
                        <li>Enunciado:
                            {% if examen.enunciado_ejercicio  is not null %}
                                <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                            {% endif %}
                            {% if examen.enunciado_test  is not null %}
                                <a href="{{ examen.enunciado_test }}">Test</a>
                            {% endif %}
                        </li>
                        <li>Soluciones:
                            {% if examen.solucion_ejercicio  is not null %}
                                <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                            {% endif %}
                            {% if examen.solucion_test  is not null %}
                                <a href="{{ examen.solucion_test }}">Test</a>
                            {% endif %}
                        </li>
                    </ul>
                </div>
                {% endif %}
                {% if examen.convocatoria == 1 %}
                    {% if partes == 0 %}
                    <div class="ml-5 mr-5 ulpgcds-tab-content" id="tab-2">
                        {% if examen.fecha  is null %}
                            <h3>Convocatoria ordinaria ({{ examen.anno }}):</h3>
                        {% else %}
                            <h3>Convocatoria ordinaria ({{ examen.fecha|date("d/m/Y") }}):</h3>
                        {% endif %}
                    {% endif %}
                        <ul class="ulpgcds-list">
                            {% if examen.parte == 1 %}
                                <li>
                                    Primera parte (temas 1, 2 y 3):
                                    <ul>
                                        <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                                <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                            {% endif %}
                                            {% if examen.enunciado_test  is not null %}
                                                <a href="{{ examen.enunciado_test }}">Test</a>
                                            {% endif %}
                                        </li>
                                        <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                                <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                            {% endif %}
                                            {% if examen.solucion_test  is not null %}
                                                <a href="{{ examen.solucion_test }}">Test</a>
                                            {% endif %}
                                        </li>
                                    </ul>
                                </li>
                            {% endif %}
                            {% if examen.parte == 2 %}
                                <li>
                                    Segunda Parte (temas 4 y 5):
                                    <ul>
                                        <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                                <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                            {% endif %}
                                            {% if examen.enunciado_test  is not null %}
                                                <a href="{{ examen.enunciado_test }}">Test</a>
                                            {% endif %}
                                        </li>
                                        <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                                <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                            {% endif %}
                                            {% if examen.solucion_test  is not null %}
                                                <a href="{{ examen.solucion_test }}">Test</a>
                                            {% endif %}
                                        </li>
                                    </ul>
                                </li>
                            {% endif %}
                            {% if examen.parte == 0 %}
                                <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                        <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                    {% endif %}
                                    {% if examen.enunciado_test  is not null %}
                                        <a href="{{ examen.enunciado_test }}">Test</a>
                                    {% endif %}
                                </li>

                                <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                        <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                    {% endif %}
                                    {% if examen.solucion_test  is not null %}
                                        <a href="{{ examen.solucion_test }}">Test</a>
                                    {% endif %}
                                </li>
                            {% endif %}
                        </ul>
                    {% if partes == 1 %}
                        </div>
                    {% endif %}
                    {% set partes = partes + 1 %}
                {% endif %}

                {% if examen.convocatoria == 2 %}
                    {% if partesExtraordinaria == 0 %}
                        <div class="ml-5 mr-5 ulpgcds-tab-content" id="tab-3">
                        {% if examen.fecha  is null %}
                            <h3>Convocatoria extraordinaria ({{ examen.anno }}):</h3>
                        {% else %}
                            <h3>Convocatoria extraordinaria ({{ examen.fecha|date("d/m/Y") }}):</h3>
                        {% endif %}
                    {% endif %}
                    <ul class="ulpgcds-list">
                        {% if examen.parte == 1 %}
                            <li>
                                Primera parte (temas 1, 2 y 3):
                                <ul>
                                    <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                            <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.enunciado_test  is not null %}
                                            <a href="{{ examen.enunciado_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                    <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                            <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.solucion_test  is not null %}
                                            <a href="{{ examen.solucion_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                </ul>
                            </li>
                        {% endif %}
                        {% if examen.parte == 2 %}
                            <li>
                                Segunda Parte (temas 4 y 5):
                                <ul>
                                    <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                            <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.enunciado_test  is not null %}
                                            <a href="{{ examen.enunciado_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                    <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                            <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.solucion_test  is not null %}
                                            <a href="{{ examen.solucion_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                </ul>
                            </li>
                        {% endif %}
                        {% if examen.parte == 0 %}
                            <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                    <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                {% endif %}
                                {% if examen.enunciado_test  is not null %}
                                    <a href="{{ examen.enunciado_test }}">Test</a>
                                {% endif %}
                            </li>

                            <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                    <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                {% endif %}
                                {% if examen.solucion_test  is not null %}
                                    <a href="{{ examen.solucion_test }}">Test</a>
                                {% endif %}
                            </li>
                        {% endif %}
                    </ul>
                    {% if partesExtraordinaria == 1 %}
                        </div>
                    {% endif %}
                    {% set partesExtraordinaria = partesExtraordinaria + 1 %}
                {% endif %}

                {% if examen.convocatoria == 3 %}
                    {% if partesEspecial == 0 %}
                        <div class="ml-5 mr-5 ulpgcds-tab-content" id="tab-4">
                        {% if examen.fecha  is null %}
                            <h3>Convocatoria especial ({{ examen.anno }}):</h3>
                        {% else %}
                            <h3>Convocatoria especial ({{ examen.fecha|date("d/m/Y")}}):</h3>
                        {% endif %}
                    {% endif %}
                    <ul class="ulpgcds-list">
                        {% if examen.parte == 1 %}
                            <li>
                                Primera parte (temas 1, 2 y 3):
                                <ul>
                                    <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                            <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.enunciado_test  is not null %}
                                            <a href="{{ examen.enunciado_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                    <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                            <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.solucion_test  is not null %}
                                            <a href="{{ examen.solucion_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                </ul>
                            </li>
                        {% endif %}
                        {% if examen.parte == 2 %}
                            <li>
                                Segunda Parte (temas 4 y 5):
                                <ul>
                                    <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                            <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.enunciado_test  is not null %}
                                            <a href="{{ examen.enunciado_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                    <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                            <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.solucion_test  is not null %}
                                            <a href="{{ examen.solucion_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                </ul>
                            </li>
                        {% endif %}
                        {% if examen.parte == 0 %}
                            <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                    e                <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                {% endif %}
                                {% if examen.enunciado_test  is not null %}
                                    <a href="{{ examen.enunciado_test }}">Test</a>
                                {% endif %}
                            </li>

                            <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                    <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                {% endif %}
                                {% if examen.solucion_test  is not null %}
                                    <a href="{{ examen.solucion_test }}">Test</a>
                                {% endif %}
                            </li>
                        {% endif %}
                    </ul>
                    {% if partesEspecial == 1 %}
                        </div>
                    {% endif %}
                    {% set partesEspecial = partesEspecial + 1 %}
                {% endif %}

            {% endfor %}

           {# <div class="row ml-5">
            <div class="card col-3">
            {% for examen in examenes %}
                        <ul class="ulpgcds-list">
                            #}{#                    Examen parcial#}{#
                            {% if examen.convocatoria == 0 %}
                                {% if examen.fecha  is null %}
                                    <li>Examen Parcial ({{ examen.anno }}):
                                {% else %}
                                    <li>Examen Parcial ({{ examen.fecha }}):
                                {% endif %}
                                <ul class="ulpgcds-list">
                                    <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                            <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.enunciado_test  is not null %}
                                            <a href="{{ examen.enunciado_test }}">Test</a>
                                        {% endif %}
                                    </li>

                                    <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                            <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                        {% endif %}
                                        {% if examen.solucion_test  is not null %}
                                            <a href="{{ examen.solucion_test }}">Test</a>
                                        {% endif %}
                                    </li>
                                </ul>
                                </li>
                            {% endif %}
                            #}{#                    Examen ordinaria#}{#

                            {% if examen.convocatoria == 1 %}
                                {% if examen.fecha  is null %}
                                    <li>Convocatoria ordinaria ({{ examen.anno }}):
                                {% else %}
                                    <li>Convocatoria ordinaria  ({{ examen.fecha }}):
                                {% endif %}
                                        <ul class="ulpgcds-list">
                                            {% if examen.parte == 1 %}
                                                <li>
                                                    Primera parte (temas 1, 2 y 3):
                                                    <ul>
                                                        <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                                                <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                                            {% endif %}
                                                            {% if examen.enunciado_test  is not null %}
                                                                <a href="{{ examen.enunciado_test }}">Test</a>
                                                            {% endif %}
                                                        </li>
                                                        <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                                                <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                                            {% endif %}
                                                            {% if examen.solucion_test  is not null %}
                                                                <a href="{{ examen.solucion_test }}">Test</a>
                                                            {% endif %}
                                                        </li>
                                                    </ul>
                                                </li>
                                            {% endif %}
                                            {% if examen.parte == 2 %}
                                                <li>
                                                    Segunda Parte (temas 4 y 5):
                                                    <ul>
                                                        <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                                                <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                                            {% endif %}
                                                            {% if examen.enunciado_test  is not null %}
                                                                <a href="{{ examen.enunciado_test }}">Test</a>
                                                            {% endif %}
                                                        </li>
                                                        <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                                                <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                                            {% endif %}
                                                            {% if examen.solucion_test  is not null %}
                                                                <a href="{{ examen.solucion_test }}">Test</a>
                                                            {% endif %}
                                                        </li>
                                                    </ul>
                                                </li>
                                            {% endif %}
                                            {% if examen.parte == 0 %}
                                                <li>Enunciado: {% if examen.enunciado_ejercicio  is not null %}
                                                        <a href="{{ examen.enunciado_ejercicio }}">Ejercicios</a>
                                                    {% endif %}
                                                    {% if examen.enunciado_test  is not null %}
                                                        <a href="{{ examen.enunciado_test }}">Test</a>
                                                    {% endif %}
                                                </li>

                                                <li>Soluciones: {% if examen.solucion_ejercicio  is not null %}
                                                        <a href="{{ examen.solucion_ejercicio }}">Ejercicios</a>
                                                    {% endif %}
                                                    {% if examen.solucion_test  is not null %}
                                                        <a href="{{ examen.solucion_test }}">Test</a>
                                                    {% endif %}
                                                </li>
                                            {% endif %}

                                        </ul>
                                </li>
                            {% endif %}
                        </ul>

            {% endfor %}
            </div>
            </div>#}
        {% endif %}

    <footer>
        <div class="ulpgcds-footer">
            <div class="ulpgcds-footer__top wrapper float-left mb-3">
                <div class="row mx-auto">
                    <div class="col-6">
                        <div class="footer-logo">
                            <a href="/"><img src="assets/img/sopaxx.gif" alt="[S.Op.]" align="bottom" border="0"></a>
                            <a class="m-1 align-bottom" href="/design">Portada</a>
                            <p class="copyright p-2">&copy; 1994-2020 Universidad de Las Palmas de Gran Canaria</p>
                        </div>
                    </div>
                </div>
            </div>
            <div class="ulpgcds-footer__bottom">
                <div class="wrapper">
                    <p>© Universidad de Las Palmas de Gran Canaria · ULPGC</p>
                </div>
            </div>
        </div>
    </footer>

{% endblock %}

{% block title %}
    ULPGC · Exámenes resueltos desde el año 1993
{% endblock %}

{% block js %}
    <script type="text/javascript">
        $('.ulpgcds-tabs li').click(function(){
            var tab_id = $(this).attr('href');

            $(this).parent().find("li").removeClass('active');
            $('.tab-content').removeClass('active');

            $(this).addClass('active');
            if ($(tab_id))
                $(tab_id).addClass('active');
        });
    </script>
    <script type="text/javascript">
        function redirigir() {
            var desplegable = document.getElementById("dropdown");
            var anno = desplegable.value;
            window.location.replace('/bbdd/' + anno);
        }
    </script>
    <script src="http://www.google-analytics.com/urchin.js" type="text/javascript">
    </script>
    <script type="text/javascript">
        _uacct = "UA-1660551-1";
        urchinTracker();
    </script>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script type="text/javascript" src="https://cdn.ulpgc.es/ulpgcds/1.0/js/ulpgcds.js?v=1.0"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/a5b87c29a1.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootbox.js/5.4.0/bootbox.min.js"></script>
    <link href="https://cdn.ulpgc.es/ulpgcds/1.0/js/chosen/chosen.css" media="all" rel="stylesheet" />
    <script src="https://cdn.ulpgc.es/ulpgcds/1.0/js/chosen/chosen.jquery.js"></script>

{% endblock %}